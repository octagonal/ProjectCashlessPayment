<UserControl x:Class="nmct.ba.cashlessproject.management.View.Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:nmct.ba.cashlessproject.management.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <vm:LoginVM/>
    </UserControl.DataContext>
    
    <StackPanel>
        <TextBlock Text="Login:" Margin="4"/>
        <TextBox Text="{Binding Username}" Margin="8"/>
        <TextBlock Text="Password:" Margin="4"/>
        <TextBox Text="{Binding Password}" Margin="8"/>
        <Button Content="Aanmelden" Command="{Binding LoginCommand}" Margin="8"/>
        <TextBlock Foreground="Red" Text="{Binding Error}"/>
    </StackPanel>
</UserControl>
